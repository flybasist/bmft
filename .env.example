# ============================================================================
# BMFT Bot Configuration (.env)
# ============================================================================
# Скопируй этот файл в .env и настрой под свой сценарий развёртывания
# Команда: cp .env.example .env
# ============================================================================

# ============================================================================
# ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ
# ============================================================================

# Telegram Bot Token (получить у @BotFather)
# ВАЖНО: Этот токен одинаковый для всех сценариев!
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# PostgreSQL Connection String
# КРИТИЧНО: Меняется в зависимости от сценария развёртывания!
# Формат: postgres://user:password@host:port/database?sslmode=mode
#
# ============================================================================
# ВЫБЕРИ СВОЙ СЦЕНАРИЙ (раскомментируй нужную строку):
# ============================================================================
#
# 📦 СЦЕНАРИЙ 1: Оба compose (БД + Бот в Docker)
# Команды:
#   docker-compose -f docker-compose.env.yaml -f docker-compose.bot.yaml up -d
# Почему @postgres:
#   Бот и БД в одной Docker сети, Docker DNS резолвит имя сервиса 'postgres'
POSTGRES_DSN=postgres://bmft:secret@postgres:5432/bmft?sslmode=disable
#
# 💻 СЦЕНАРИЙ 2: БД в compose, Бот нативно (go run)
# Команды:
#   docker-compose -f docker-compose.env.yaml up -d
#   go run cmd/bot/main.go
# Почему @localhost:
#   Бот ВНЕ Docker сети, порт 5432 проброшен наружу в docker-compose.env.yaml
#POSTGRES_DSN=postgres://bmft:secret@localhost:5432/bmft?sslmode=disable
#
# 🌐 СЦЕНАРИЙ 3: БД удалённая, Бот нативно (go run)
# Команды:
#   go run cmd/bot/main.go
# Почему реальный хост:
#   БД где-то в облаке/другом сервере, используй sslmode=require для безопасности
#POSTGRES_DSN=postgres://bmft_user:strong_password@db.example.com:5432/bmft?sslmode=require
#
# 🐳 СЦЕНАРИЙ 4: БД удалённая, Бот в compose
# Команды:
#   docker-compose -f docker-compose.bot.yaml up -d
# Почему реальный хост:
#   БД где-то в облаке, бот в Docker контейнере подключается через интернет
#POSTGRES_DSN=postgres://bmft_user:strong_password@db.example.com:5432/bmft?sslmode=require
#
# ============================================================================

# ============================================================================
# ОПЦИОНАЛЬНЫЕ ПАРАМЕТРЫ (с умными дефолтами)
# ============================================================================

# Уровень логирования: debug, info, warn, error
# Разработка: debug или info
# Production: warn или error
LOG_LEVEL=info

# Pretty-print логов (true = читаемо для человека, false = JSON для парсинга)
# Разработка: true (удобно читать в консоли)
# Production: false (JSON логи для Grafana/ELK)
LOGGER_PRETTY=false

# Таймаут graceful shutdown
# Сколько ждать завершения обработки сообщений перед остановкой
SHUTDOWN_TIMEOUT=15s

# Адрес HTTP-сервера метрик
# Endpoints: GET /healthz (health check), GET /metrics (Prometheus metrics)
METRICS_ADDR=:9090

# Таймаут Long Polling (секунды)
# Как долго держать соединение с Telegram API в ожидании новых сообщений
# Рекомендуется: 30-60 секунд
POLLING_TIMEOUT=60

# ============================================================================
# ПАРАМЕТРЫ ДЛЯ DOCKER-COMPOSE (опционально, есть defaults)
# ============================================================================
# Эти параметры используются ТОЛЬКО если запускаешь docker-compose.env.yaml
# Для сценариев 2, 3, 4 они НЕ нужны (есть defaults в compose файле)

# PostgreSQL credentials (для docker-compose.env.yaml)
POSTGRES_USER=bmft
POSTGRES_PASSWORD=secret
POSTGRES_DB=bmft

# PostgreSQL external port (для docker-compose.env.yaml)
# Порт на хост-машине для подключения извне (например, из IDE или go run)
POSTGRES_PORT=5432
