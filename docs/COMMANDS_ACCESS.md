# 📋 Команды и уровни доступа

Полный справочник всех команд бота с указанием уровней доступа.

---

## 🔑 Условные обозначения

- 👤 **USER** — доступно всем пользователям
- 👑 **ADMIN** — доступно только администраторам чата
- 🎯 **CONTEXT** — поведение зависит от контекста (топик / основной чат)

---

## 📊 Statistics Module

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/mystats` | 👤 USER | Показать вашу статистику за сегодня |
| `/myweek` | 👤 USER | Статистика за последние 7 дней |
| `/chatstats` | 👑 ADMIN | Статистика всего чата за сегодня |
| `/topchat` | 👑 ADMIN | Топ активных пользователей чата |

---

## 🚦 Limiter Module

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/limiter` | 👤 USER | Справка по модулю лимитов |
| `/mystats` | 👤 USER | Показать ваши текущие лимиты (дублирует Statistics) |
| `/getlimit` | 👤 USER | Посмотреть текущие лимиты чата |
| `/setlimit <тип> <число>` | 👑 ADMIN | Установить лимит на тип контента |
| `/setvip @username` | 👑 ADMIN | Выдать VIP-статус пользователю |
| `/removevip @username` | 👑 ADMIN | Снять VIP-статус |
| `/listvips` | 👤 USER | Список всех VIP-пользователей |

**Доступные типы лимитов:**
- `text`, `photo`, `video`, `sticker`, `animation`
- `voice`, `video_note`, `audio`, `document`
- `location`, `contact`

**Примеры:**
```
/setlimit photo 10
/setlimit sticker 20
/setlimit text 0     # отключить лимит
/setvip @username
```

---

## 🤖 Reactions Module

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/reactions` | 👤 USER | Справка по модулю автореакций |
| `/addreaction <паттерн> <ответ>` | 👑 ADMIN | Добавить автоматическую реакцию |
| `/listreactions` | 👑 ADMIN | Список всех активных реакций |
| `/removereaction <ID>` | 👑 ADMIN | Удалить реакцию по ID |

**Расширенный синтаксис `/addreaction`:**

```
/addreaction <паттерн> <ответ> [параметры]
```

**Доступные параметры:**
- `--regex` — использовать регулярное выражение
- `--cooldown <секунды>` — кулдаун между срабатываниями
- `--daily-limit <число>` — максимум срабатываний в день
- `--delete-on-limit` — удалять триггеры при превышении лимита
- `--user <user_id>` — реакция только для конкретного пользователя
- `--content-type <тип>` — реакция только на определённый тип контента

**Типы ответов:**
- `text:` — текстовое сообщение
- `sticker:` — стикер (file_id)
- `photo:` — фото (file_id)
- `video:` — видео (file_id)
- `animation:` — GIF (file_id)
- `document:` — документ (file_id)

**Примеры:**
```
/addreaction привет text:Здравствуй!
/addreaction добр.* утр text:Доброе утро! --regex
/addreaction cat sticker:CAACAgIAAxkBAAEM...
/addreaction мем photo:AgACAgIAAxkBAAEM... --cooldown 3600
/addreaction фото text:Крутое фото! --content-type photo --daily-limit 10
```

---

## ⏰ Scheduler Module

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/scheduler` | 👤 USER | Справка по модулю планировщика |
| `/addtask <cron> <текст>` | 👑 ADMIN | Добавить запланированную задачу |
| `/listtasks` | 👑 ADMIN | Список всех настроенных задач |
| `/deltask <ID>` | 👑 ADMIN | Удалить задачу по ID |

**Формат cron:**
```
* * * * *
│ │ │ │ └─ день недели (0-6, 0=воскресенье)
│ │ │ └─── месяц (1-12)
│ │ └───── день месяца (1-31)
│ └─────── час (0-23)
└───────── минута (0-59)
```

**Примеры:**
```
/addtask 0 9 * * * Доброе утро! ☀️        # каждый день в 9:00
/addtask 0 18 * * 1-5 Рабочий день окончен  # пн-пт в 18:00
/addtask 0 12 * * 6,0 С выходными! 🎉      # сб-вс в 12:00
/addtask 0 */6 * * * Напоминание           # каждые 6 часов
```

---

## 🚫 TextFilter Module

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/textfilter` | 👤 USER | Справка по модулю фильтра текста |
| `/addban <паттерн> [действие]` | 👑 ADMIN | Добавить бан-слово или фразу |
| `/listbans` | 👑 ADMIN | Список всех активных фильтров |
| `/removeban <ID>` | 👑 ADMIN | Удалить бан-слово по ID |

**Действия:**
- `delete` — просто удалить сообщение (по умолчанию)
- `warn` — предупредить пользователя (сообщение остаётся)
- `delete_warn` — удалить сообщение И отправить предупреждение

**Примеры:**
```
/addban спам delete
/addban реклама delete_warn
/addban оскорбление warn
/removeban 7
```

---

## 🔞 ProfanityFilter Module

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/profanity` | 👤 USER | Справка по модулю фильтра мата |
| `/setprofanity [действие]` | 👑 ADMIN | Включить фильтр ненормативной лексики |
| `/removeprofanity` | 👑 ADMIN | Отключить фильтр мата |
| `/profanitystatus` | 👤 USER | Проверить статус фильтра |

**Действия:**
- `delete` — удалить сообщение с матом (по умолчанию)
- `warn` — предупредить пользователя
- `delete_warn` — удалить и предупредить

**Примеры:**
```
/setprofanity delete
/setprofanity delete_warn
/removeprofanity
```

**⚠️ Особенности:**
- VIP-пользователи не попадают под фильтр мата
- Фильтр работает независимо для каждого топика
- Использует встроенный словарь ненормативной лексики

---

## 🎯 Работа с топиками

Все модули поддерживают работу с топиками (темами) в группах:

### Контекстное поведение:

**Команда в топике:**
- Настраивает модуль только для этого топика
- Все ограничения и фильтры действуют только внутри топика

**Команда в основном чате:**
- Настраивает модуль для всего чата
- Действует как глобальные настройки

**Fallback логика:**
- Если для топика настройки отсутствуют → используются настройки чата
- Если для чата настройки отсутствуют → используются дефолтные значения

**Примеры:**

```
# В топике "Мемы":
/setlimit photo 50          # разрешено 50 фото только в топике "Мемы"

# В основном чате:
/setlimit photo 10          # разрешено 10 фото во всём чате (кроме топика "Мемы")

# В топике "Работа":
/setprofanity delete        # мат запрещён только в топике "Работа"

# В топике "Новости":
/addtask 0 9 * * * Утренний дайджест 📰  # сообщение только в топике "Новости"
```

---

## 🛡️ VIP-статус

VIP-пользователи получают особые привилегии:

✅ **Игнорируют:**
- Все лимиты модуля Limiter
- Фильтр ненормативной лексики (ProfanityFilter)

⚠️ **Не игнорируют:**
- TextFilter (бан-слова по-прежнему действуют)
- Модерацию администраторов

**Управление VIP:**
```
/setvip @username           # через упоминание
/setvip                     # ответить на сообщение пользователя
/removevip @username
/listvips                   # посмотреть всех VIP
```

---

## ⚡ Быстрая справка

### Для обычных пользователей:

```
/mystats     — моя статистика
/myweek      — статистика за неделю
/getlimit    — текущие лимиты чата
/listvips    — список VIP-пользователей
```

### Для администраторов:

```
# Модерация контента
/setlimit <тип> <число>        # установить лимит
/addban <слово> [действие]     # забанить слово
/setprofanity [действие]       # включить фильтр мата

# Автоматизация
/addreaction <паттерн> <ответ> # настроить автоответ
/addtask <cron> <текст>        # запланировать сообщение

# Статистика
/chatstats   — статистика чата
/topchat     — топ активных пользователей

# VIP-управление
/setvip @username
/removevip @username
```

---

## 🔗 См. также

- [Полное описание модулей](modules/MODULES.md) — детальная документация каждого модуля
- [Быстрый старт](QUICKSTART.md) — пошаговая установка бота
- [Архитектура БД](architecture/DATABASE.md) — структура базы данных
- [Главная документация](README.md) — навигация по документации

---

**Версия документа:** 1.0  
**Последнее обновление:** 2025  
**Проект:** BMFT (Better Moderation & Fun Tools)
